import { launchTestNode, TestAssetId } from 'fuels/test-utils';

import { describe, test, expect } from 'vitest';

describe('Counter contract', () => {
  test('All new callers have initial counter set to zero', async () => {
    // ...
  });
  test('Increasing counter of one caller does not affect counters of other callers', async () => {
    // ...
  });
  test('Decreasing counter below zero reverts', async () => {
    // ...
  });
});
